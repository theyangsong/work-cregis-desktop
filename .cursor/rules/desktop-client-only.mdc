---
description: work-cregis-desktop 是 Desktop 客户端 — 禁止 Website / Showcase 壳层，100% 只用 @eds/desktop-*
globs: **/*
alwaysApply: true
---

# Desktop 客户端 — 禁止 Website

**在 work-cregis-desktop 内写代码或排查样式时，先读仓库根目录 `AGENTS.md`。**

- 只用 `@eds/desktop-tokens`、`@eds/desktop-components`。
- 禁止 `@eds/website-*`；禁止从 eds-desktop showcase 复制 CSS / token。
- 排版变量必须在 `packages/tokens/spec` 中存在（如 `--eds-footnote-size`，勿用 `--eds-footnote-medium-*`）。
- 同步 eds-desktop = 只消费 `packages/tokens` + `packages/components`，不同步 showcase。
- **对齐引用库**：DS 行为以 `../eds-desktop/packages/**` 为准；业务 `TasksListField*` 对齐 Showcase 集成，勿保留已废弃 props、勿手搓 Tooltip。

Showcase 预览可能靠 Website 壳继承 token；**本项目没有这层兜底。**
