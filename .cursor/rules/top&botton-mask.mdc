---
description: 【强推】滚动顶底毛玻璃遮罩 Top & Bottom Mask — 禁止手搓；须 EgSkid 或 useScrollChromeScrim + effect-mask
globs: src/**/*
alwaysApply: true
---

# Top & Bottom Mask — 业务侧强推（work-cregis-desktop）

> **权威详案（真源）**：`../eds-desktop/.cursor/rules/top&botton-mask.mdc`  
> **DS 实现参考**：`../eds-desktop/packages/components/src/templates/skid/Skid.vue`  
> **本仓库 composable**：`src/scenes/tasks/shared/useScrollChromeScrim.ts`  
> **work 集成摘要**：`.cursor/rules/work.mdc` §6.5

---

## 硬约束（违反即错）

1. **禁止**在 `src/**` 自造「fixed header + 内层滚动」或单独给顶栏写 `backdrop-filter` 而不走下列路径之一。
2. **禁止**毛玻璃层使用不透明 `var(--box-page)` / hex；开 scrim 时须 `var(--effect-mask)` + `var(--eds-blur-bg)`。
3. **禁止**在业务 CSS 覆盖 `EgSkid` / DS sticky 顶底的 `background`（会废掉 blur）。
4. **须**单滚动容器：`overflow-y: auto` 的一个节点包住 sticky 顶 + 正文 + sticky 底；`height: 100%` + `min-height: 0` 不断链。
5. **须**按滚动位置切换 scrim（见状态机）；**顶部未滚动时顶栏为实色是正确行为**，不是 bug。
6. 祖先 **禁止** `transform` / `filter` / `opacity < 1` 包住滚动区（会破坏 `backdrop-filter`）。

---

## 业务侧只允许两条路径

| 优先级 | 场景 | 做法 |
|--------|------|------|
| **1** | Layout 右侧 Skid / 标准滑层 | `<EgLayout #skid>` + `<EgSkid>` default slot；**不要**外包自定义顶栏 |
| **2** | Popup / Flotation 内自定义列表壳 | `useScrollChromeScrim(scrollRef, { contentRef })` + `topScrim` / `bottomScrim` 绑 class；视觉 token 同 DS |

**已有落地示例（必读）：**

- `SigningBatchNetworkPickerMenu.vue` — Flotation 列表顶栏 + bottom fade
- `SigningBatchPopupSlotChrome.vue` — Popup 底栏 `::before` scrim

---

## 状态机（与 DS 一致）

```text
canScroll     = scrollHeight - clientHeight > 2
topScrim      = canScroll && scrollTop > 2
bottomScrim   = canScroll && scrollTop + clientHeight < scrollHeight - 2
```

| 位置 | 顶栏 | 底栏 |
|------|------|------|
| 不可滚 | 实色 | 实色 |
| 顶部 | 实色 | scrim（若可滚） |
| 中间 | scrim | scrim |
| 底部 | scrim | 实色 |

---

## 视觉（锁死）

```css
/* 直接挂 sticky 节点（同 .eds-frosted-page-chrome） */
background: var(--effect-mask);
-webkit-backdrop-filter: var(--eds-blur-bg);
backdrop-filter: var(--eds-blur-bg);
```

Popup 底栏伪元素写法见 `eds-desktop` `popupChromeScrim.module.css` 与本仓库 `SigningBatchPopupSlotChrome.vue`。

---

## 排查「毛玻璃做不出来」

| 现象 | 先查 |
|------|------|
| 完全不糊 | 是否滚动了；是否滚错容器；token 是否加载 |
| 只有灰块 | 背景是否不透明；业务 CSS 是否盖住 DS |
| 永远不触发 | 容器有无溢出；`canScroll` 是否为 false |
| Skid 不对 | 是否用了裸 `<EgSkid>` 却又在外层加了 fixed 顶栏 |

**验收对照**：eds-desktop Showcase `/components/skid` → 勾选「显示测试内容」→ 滚动。

---

## Agent

- 用户要顶底毛玻璃 → **先读** `../eds-desktop/.cursor/rules/top&botton-mask.mdc`，再改业务；优先 `EgSkid` 或复用 `useScrollChromeScrim`。
- **禁止**为修效果改 `../eds-desktop/packages/**`（须切换 eds-desktop 仓库）。
- 需改 mask 不透明度 → 只改 eds-desktop `effect-mask` token，再「同步 eds-desktop」。
