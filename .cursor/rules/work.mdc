---
description: work-cregis-desktop 唯一业务规范 — Desktop 客户端、DS 边界、i18n、顶底毛玻璃、Popup、场景定稿
globs: **/*
alwaysApply: true
---

# work-cregis-desktop 业务集成规范

> **项目**：Desktop 客户端（非 Showcase / 非 Website 壳）。  
> **入口**：改代码前先读根目录 `AGENTS.md`。  
> **DS 权威**：`../eds-desktop/.cursor/rules/eds-project.mdc`、`packages/tokens/spec/**`。  
> **Showcase 无兜底**：Showcase 外层 Website 壳会继承无效 token；本仓库无效 token 直接失效。


## 0. 速查索引

| 主题 | 章节 |
|------|------|
| Agent 工作思路（先思考再动手） | 文首 |
| 禁止复制、Desktop-only | §1 |
| 本仓库 vs eds-desktop、同步、dev HMR、Git 推送 | §2 |
| DOM、EgPopup 挂载位置 | §3 |
| Eg* 组件用法 | §4 |
| UI 文案 i18n | §5 |
| Token、千分位、顶底毛玻璃、网络名称 | §6 |
| EgPopup 生命周期、批处理、多签、钱包列、Shell Debug、DataList 批选动效 | §7 |
| DataList 列 min-width / flex 分配 | §7.7 |
| lint、PR 清单 | §8 |
| 文件索引 | §9 |

---

## 1. 全局硬约束

### 1.1 Desktop-only

**【必须】** 只使用 `@eds/desktop-tokens`、`@eds/desktop-animations`、`@eds/desktop-components`。

**【禁止】**

| 项 | 原因 |
|----|------|
| `@eds/website-*` | 与 Desktop token 体系不同 |
| 从 `eds-desktop/apps/showcase/**` 复制 CSS / 集成 / token 名 | Showcase 靠 Website 壳继承变量 |
| Desktop spec 不存在的 token（如 `--eds-footnote-medium-*`） | 正确名为 `--eds-footnote-*` |
| `@import '@eds/desktop-components/style.css'`（dist 快照） | dev/build 须 alias 到 DS 源码 |

排版 token 查：`../eds-desktop/packages/tokens/spec/typography/semantic.json`。

### 1.2 禁止复制

**【禁止】** 向用户提供任何复制能力：`showValueCopy`、`EgListFieldHashLikeLine` 复制、`CryptoAddressSide` 复制、`navigator.clipboard`、复制按钮/Menu。

**【必须】** 哈希/编号/地址/金额只读展示；溢出用 `EgListFieldOverflowText` / `EgTextOverflowTooltip`（无复制）。`copyOnRowHover` 保持 `false`。

**【禁止】** 业务区 CSS 设 `user-select: text` 绕过 shell（输入框 / `textarea` / `contenteditable` 除外，`global.css` 已白名单）。

### 1.3 样式与组件总则

**【必须】** 样式用 Desktop 语义 token + 组件 props；交互优先 DS 组合（如 `EgFlotation` Combo）。

**【禁止】** 裸 `px`/hex/`font-weight: 数字`；私造 `--*`；用 `--scale-*` 冒充 `--radius-md` 等语义 token；覆盖 `Eg*` 根节点 `class`/`style` 或内部 DOM。

**【必须】** `var(--*)` 仅在 `@eds/desktop-tokens/json` 白名单或 `token-allowlist.business.json` 已批条目内；否则 `pnpm lint:ds-tokens` 失败。缺 token → 向 eds-desktop 申请，**不得**私造。

---

## 2. 仓库边界与工程

### 2.1 DS ↔ 业务单向边界

**方向**：eds-desktop 改 → 本 App 跟；本 App 改 → 不写回 eds-desktop。

| 本仓库可改 | 本仓库禁止改 |
|------------|--------------|
| `src/**` | `../eds-desktop/packages/**` |
| `token-allowlist.business.json` | `../eds-desktop/apps/showcase/**` |
| `scripts/verify-*.mjs` | 把业务逻辑抄进 DS 包 |

改 Popover / token / 组件 API → **切换 eds-desktop 仓库**改，或告知用户属 DS 范围。

**消费 DS**：`link:../eds-desktop/packages/{tokens,animations,components}`；Vite alias `@eds/desktop-components` / `@eds/desktop-animations` → DS 源码。

**同步 eds-desktop**（仅用户明确要求时）：`../eds-desktop` build → 本仓库 `pnpm install` → **重启 dev**。只同步 `packages/tokens`、`packages/components` 与可 sync 的 list-field 辅助；不含 showcase/Website。**无反向 sync**。

### 2.2 对齐引用库

**【必须】** DS 行为以 `../eds-desktop/packages/**` 为准；Showcase `ListFieldPreviewPanel.vue` 仅作集成参考。

| 变更类型 | 改哪里 |
|----------|--------|
| Tooltip、BatchBar、DataList 等 DS 行为 | `packages/components` |
| `TasksListField*` 薄封装 | 业务 `src/**`，对齐 Showcase 选型 |

**【必须】** 所有可读文本（含哈希/编号/地址）用 `EgListFieldOverflowText`。**【禁止】** `EgListFieldHashLikeLine` / `CryptoAddressSide` 复制 Menu。

### 2.3 dev 实时生效

**【必须】** UI/样式/交互改后，在 `pnpm dev` 下保存即 HMR/full-reload 可见。**【禁止】** 只 `pnpm build` 不 dev 就当验收完成。

| 范围 | Agent 动作 |
|------|------------|
| `src/**` | 确认 dev 在跑；未更新 → 硬刷新 → 重启 dev |
| `../eds-desktop/packages/**` | 走 alias 源码；**禁止**改 `dist` |
| 同步 eds-desktop | build + install + 重启 dev（≠ 日常 HMR） |

### 2.4 list-field 本地扩展（sync 禁止覆盖）

`src/scenes/tasks/list-field/listFieldCryptoSampleAddresses.ts` 含业务专用导出：`getPinnedAddressForRow`、`ton`/`sui`、`familyOverride`、`DEMO_ADDRESS_POOL_MIN = 23`。

**【禁止】** showcase 同名文件整文件替换（sync 已 skip）。**【必须】** 保留 marker；`node scripts/verify-list-field-crypto.mjs` 校验。Tasks 整页空白 → 先查此文件。

### 2.5 Git 提交与推送（双仓库 + Pages CI）

本仓库 Pages CI（`.github/workflows/deploy-pages.yml`）会 **按 SHA pin** 检出远程 `theyangsong/evergreen-design-system-desktop`。本地未推的 DS 提交、或 pin 指向远程不存在的 SHA，都会让 Deploy 失败。

**【禁止】** Agent 主动执行 `git push`（含 `--force`、upstream 首次推送）。用户说「推」之后：**只** 准备 commit 说明、预检命令、以及用户可复制的 `git push` 命令；**由用户在自己的终端推送**。

**【必须】** 用户要求推送时，视为「截止当前、两个仓库一起最新」：

| 仓库 | 必须包含 |
|------|----------|
| `../eds-desktop` | 本次相关的 DS 改动已提交到 **远程** `main`（`.vscode/` 等 IDE 文件默认不提交） |
| 本仓库 | 业务改动 + CI 里 `Checkout eds-desktop` 的 `ref` = **该远程最新 SHA** |

**【禁止】** 只改 pin、不把对应 DS 本地改动推到远程；**【禁止】** pin 用仅存在于本机的 SHA。

**【必须】** 推送前一次验完，再交给用户推；**【禁止】** 推完再靠反复修 CI 补洞。至少：

1. 两仓库工作区干净（仅允许用户明确排除的未跟踪文件，如 `.vscode/`）。
2. `pnpm typecheck`（及本次改动相关的 lint / 业务校验）。
3. **4173 / 4174 一致**（见下「双端口 parity」）后再预览 4174。
4. 确认 pin SHA 在远程存在：`git ls-remote` / `git fetch` 后 `git cat-file -e <sha>`。

**双端口 parity（发包 / 推送前【必须】）**

| 端口 | 含义 |
|------|------|
| **4173** | `pnpm dev` — 当前 **HEAD** + 工作区（HMR 实时） |
| **4174** | `pnpm preview:pages` — 静态 `dist/`，须与待推 **HEAD** 同内容 |

- **【必须】** 用户说「推 / 发包 / 发布 Pages」时，Agent 主动核对：`dist/deploy-manifest.json` 的 `sha` === `git rev-parse HEAD`，且 eds-desktop 本地 SHA === CI pin；不一致则 **顺序** 重建 4174（`pnpm preview:pages`），**禁止** 与 `pnpm dev` 并行跑 prebuild / predev（会撞 `build:animations`）。
- **【必须】** 4174 重建后确认 `node scripts/verify-pages-artifact.mjs` 通过；4173 与 4174 对照同一 commit、同一 eds-desktop SHA，仅 `base` 与 dev/prod 模式差异属预期。
- **【禁止】** 工作区仍有待提交改动却只用 4173 目测就当发包完成；须先 commit 到干净 HEAD，再重建 4174。

用户未要求 commit / push 时，**不得** 提交或推送。

---

## 3. DOM 与挂载

```
#app
└─ .app-preview.desktopTokens     ← Desktop token 边界；EgTooltip container
   ├─ EgContainer → EgLayout → 业务页
   └─ AppPopupOverlayHost         ← 与 EgContainer 同级
```

| 规则 | 说明 |
|------|------|
| **【必须】** `.desktopTokens` 作包裹层 | **【禁止】** 打在 `Eg*` 根节点上 |
| **【必须】** `EgPopup` host 与 `EgContainer` 同级 | 遮罩覆盖整块 `.app-preview` |
| **【禁止】** Popup 挂在列表页 subtree 内 | 遮罩会被裁切 |
| **【必须】** 懒挂载 Popup 用 `usePopupShellLifecycle` | 见 §7.1 |

启动链见 `main.ts`：`initLiquidGlass` → `initEdsDesktopRuntime` → `initThemeProvider`；样式 `src/styles/desktop-components-scope.css`（import DS `global.css` + `interactionHoverTooltip.css`，**不** import dist 全量 CSS）。

---

## 4. 组件契约

**允许**：语义 props（`size`/`tone`/`variant`）、`v-model`、具名 slot。

**【禁止】** 手搓 `AnchoredTooltip + 菜单` 代替 `EgFlotation`；拆改组件内部 DOM。

**Tooltip 默认 `maxWidth`（勿传 `maxWidth` 覆盖）**

| 组件 | 默认 |
|------|------|
| `EgListFieldOverflowText` / `EgTextOverflowTooltip` | 360 |
| `EgDataListCellOverflow` header / cell | 240 / 480 |

---

## 5. UI 文案 i18n

### 5.1 五条规则

1. **【必须】** zh-cn 真源：`src/i18n/uiTextZhCN.ts`。en/zh-TW 仅来自 Excel + `collaborationLocaleOverrides.ts`。**【禁止】** Agent 自行翻译。
2. **【必须】** key 不在 `I18N_DOC_ALIGNED_KEYS`（表外）→ en / zh-TW / zh-CN **均显示 catalog 简体中文**（便于核查缺失）。
3. **【必须】** 所有 UI 固定文案走 `useAppI18n().ui()` 或 `translateUiText()`：列头、按钮、Tag、弹窗标题、tooltip、popover、toast、aria-label、placeholder。
4. **【禁止】** 对字段 **value** 调 `ui()`：人名、地址、哈希、编号、金额、时间、设备值、用户输入、Remark。
5. **en 大小写**（以 Excel / `I18N_DOC_EN_LABELS` 为准，文档已有词不二次改）：标签 Title Case；段落 Sentence case；`MPC`/`TxID` 等保持原文。

### 5.2 标签 vs 数据

| 走 `ui()` | 不走 `ui()` |
|-----------|-------------|
| Detail **title**、列头、Tag 文案 | Detail **value**、单元格姓名/地址/金额 |
| Popover 标题、按钮 | 用户备注、交易 hash |

```vue
<span>{{ ui('Device Type') }}</span>
<span>{{ deviceInfo.deviceType }}</span>
```

### 5.3 文件与同步

| 文件 | 作用 |
|------|------|
| `uiTextZhCN.ts` | zh-cn catalog（勿随意改值） |
| `uiTextZhTW.ts` | Excel 同步填充 |
| `i18nDocAlignedKeys.ts` | 表内 key 集合 |
| `i18nDocEnLabels.ts` | catalog key ≠ 文档 en-us 时的 en 映射 |
| `translateUiText.ts` | 运行时唯一入口 |
| `i18n-alignment-report.json` | 对齐报告 |

同步：`python3 scripts/sync-i18n-from-excel.py`（**不写** `uiTextZhCN.ts`）。

**【禁止】** UI 标签硬编码中英；**【禁止】** `ui(member.name)`。同义 zh-cn 不同 en → 用 `I18N_DOC_EN_LABELS`，**勿**用 `COLLABORATION_KEY_RENAMES` 合并。

### 5.4 Alert 弹窗

**【必须】** `EgPopup uses="dialog" dialog-type="standard"`。**【禁止】** `uses="reminder"`（已废弃）。

---

## 6. 样式与展示

### 6.1 Token

查：`eds-desktop/packages/tokens/spec/scale/semantic.json`、`typography/semantic.json`、`@eds/desktop-tokens/json`。

**【必须】** 颜色 `var(--material-*)` / `var(--text-*)` / `var(--stroke-*)`；排版整组角色 token；hover 用 `var(--motion-recipe-hover)`；click/focus/selected **禁止** transition。

### 6.2 数字千分位

**【必须】** 展示态数字千分位：`formatGroupedNumber`（DS）或 `src/utils/formatGroupedDisplay.ts`（金额/复合/阈值/矿工费/延迟）。

**【禁止】** 手写 `toLocaleString`；格式化输入态 `v-model`、倒计时 `MM:SS`、ID（`SIG-*`）。

| 函数 | 用途 |
|------|------|
| `formatGroupedDecimalAmount` | 金额，小数原样保留 |
| `formatGroupedAmountText` | 复合文案内数字 |
| `formatGroupedThresholdString` | 多签阈值 `3 / 4` |
| `formatGroupedLatencyLabel` | `2100ms` → `2,100ms` |

### 6.3 顶底毛玻璃（Top & Bottom Mask）

**DS 真源**：`../eds-desktop/.cursor/rules/eds-project.mdc` §13、`Skid.vue`。  
**本仓库 composable**：`src/scenes/tasks/shared/useScrollChromeScrim.ts`。  
**示例**：`SigningBatchNetworkPickerMenu.vue`、`SigningBatchPopupSlotChrome.vue`。

**【禁止】**

- `src/**` 自造 fixed header + 内层滚动，或单独顶栏 `backdrop-filter`
- scrim 用不透明 `var(--box-page)` / hex（须 `var(--effect-mask)` + `var(--eds-blur-bg)`）
- 业务 CSS 覆盖 `EgSkid` / sticky 顶底 `background`
- 祖先 `transform` / `filter` / `opacity < 1` 包住滚动区

**【必须】** 仅两条路径：

| 场景 | 做法 |
|------|------|
| Layout Skid | `<EgLayout #skid>` + `<EgSkid>`，不外包自定义顶栏 |
| Popup / Flotation 自定义壳 | `useScrollChromeScrim` + `topScrim` / `bottomScrim` |

**【必须】** 单滚动容器：`overflow-y: auto` 包住 sticky 顶 + 正文 + sticky 底；`height: 100%` + `min-height: 0`。

**状态机**（`ε = 2px`）：

```text
canScroll   = scrollHeight - clientHeight > 2
topScrim    = canScroll && scrollTop > 2
bottomScrim = canScroll && scrollTop + clientHeight < scrollHeight - 2
```

| 滚动位置 | 顶栏 | 底栏 |
|----------|------|------|
| 不可滚 | 实色 | 实色 |
| 顶部 | 实色 | scrim |
| 中间 | scrim | scrim |
| 底部 | scrim | 实色 |

**顶栏未滚动时为实色是正确行为，不是 bug。**

```css
background: var(--effect-mask);
-webkit-backdrop-filter: var(--eds-blur-bg);
backdrop-filter: var(--eds-blur-bg);
```

Flotation 底缘虚化：`bottomScrim` → 滚动容器 `mask-image` bottom fade。改 mask 不透明度 → 只改 eds-desktop `effect-mask` token，再同步。

### 6.4 网络名称

**【必须】** 链 Tag / 网络展示用 **链全名**；币种只在金额列。**【禁止】** `BSC`、`USDT-Ethereum`、`BTC-Bitcoin` 等缩写或拼接。

| 场景 | 名称约定 | 权威文件 |
|------|----------|----------|
| 列表 chain Tag | `Ethereum`、`Bitcoin`、`BNB Smart Chain`… | `tasksListFieldCurrencyRowPresets.ts` |
| 批处理分组 | `Ethereum Mainnet`、`The Open Network`、`Tron`、`Bitcoin` | `batchNetworkGroup.ts` |
| 审批金额列 | `USDT-BNB Smart Chain` 为**金额列**格式，非 Tag | `buildApprovalDetailRowFields.ts` |

---

## 7. 架构与场景定稿

### 7.1 EgPopup

**真源**：`src/scenes/tasks/shared/usePopupShellLifecycle.ts`  
**示例**：`ApprovalFlowHost.vue`、`SigningFlowHost.vue`、`*DetailPopup.vue`、`TradePasswordVerifyPopup.vue`

#### 逻辑态 vs 视觉态

| 变量 | 含义 |
|------|------|
| flow `detailOpen` / `verifyOpen` | 逻辑态（业务应保持打开） |
| EgPopup `v-model:open` | 视觉态（lifecycle 驱动） |

#### lifecycle 契约

```typescript
usePopupShellLifecycle({
  open,
  suspended?,      // 上层 Popup 打开时为 true
  onBeforeOpen?,
  onClosed,         // EgPopup 出场结束后清数据、emit update:open false
});
```

| 阶段 | 行为 |
|------|------|
| 入场 | `open && !suspended` → mount → `nextTick` → `rAF` → `popupOpen=true` |
| 真关闭 | `open=false` → `popupOpen=false` → 出场 → `@close` → `onClosed()` |
| 内联关闭（关闭钮/取消） | **仅** `popupOpen=false` |
| 暂挂 | `open && suspended` → instant 卸 shell，**不调** `onClosed` |
| watch | `{ immediate: true }` |

**【禁止】** `visibility:hidden` 暂挂下层；内联关闭时同时 `emit('update:open', false)`（关闭钮与点遮罩动效不一致）；上层打开时 `v-if` 卸下层。

#### suspend 映射

| 下层 | `shellSuspended` |
|------|------------------|
| Detail | `verifyOpen` \|\| `viewMoreOpen`（Signing 另加 progress/multiSign） |
| 地址查看更多 | 其上有 Verify（Signing 另加 progress/multiSign） |

#### Popup 尺寸【定稿】

| uses | 尺寸 |
|------|------|
| `verify` + `single-trade-password` | 328×416 |
| `detail` | 880×620 |
| `custom` md / lg / sm | 780×560 / 880×620 / 656×516（`batchSigning.constants.ts`） |

**【禁止】** 自订 `box-width`/`box-height`；照抄 Showcase `PopupPreview` 常驻挂载（业务须懒挂载 + lifecycle）。

#### 其它 Popup 定稿

- Remark Popover 宽 **336**（`POPOVER_PRESET_WIDTH_BASE`），**禁止** 256
- 矿工费多页切换：**必须** `useMotionLayoutDeformPageSwitch`；glue CSS 已在 `desktop-components-scope.css`
- Popover 展开时触发按钮保持点按态（`remarkPopoverTrigger.module.css`）
- Detail 须 `toolbar-divider-pinned`；验证成功翻下一条：`pendingAutoAdvanceId` → Verify 关闭后再 `loadDetail`；`:toolbar-page-key="detail.id"`

### 7.2 批量签名 Signing Batch

**范围**：`src/scenes/tasks/signing/batch/**`。Mock 密码：`111111`。

**入口**：有 `activeTask` → 进度页；已在多选 → toggle 退出；多币种 → `EgFlotation` + `SigningBatchNetworkPickerMenu`（**禁止**居中 EgPopup）；单币种直进多选。

**【定稿】** Sign 确认/进度弹窗：`uses=custom` **780×560** + `SigningBatchPopupSlotChrome`，挂 `AppPopupOverlayHost`。

**币种选择 Flotation 要点**

| 项 | 定稿 |
|----|------|
| `#trigger` | 仅 `batchProIconSlot` 标 `data-eds-trigger-metrics`；**禁止**整颗 `EgIconButtonPro` button |
| 尺寸 | 410×440 max（`batchSigning.constants.ts`） |
| 滚动 | `networkPickerList` + `useScrollChromeScrim`；顶未滚时标题实色 |
| 过滤 | 全量 `sortedDataList` 按 `currencyKey`，**禁止**只滤当前页 |
| 行点击 | 整行可点；图标宿主 `--avatar-md` + `EgCrypto fit` |

**批处理复查【禁止回退】**

| 规则 | 正确 | 错误 |
|------|------|------|
| 密码取消 | 只关 `verifyOpen`，保留 pending | `resetPendingBatchState` |
| 批处理按钮 | 再点退出多选 | 多选中失效 |
| 进度重入 | 任意 `activeTask` 可开 | 仅 `running` |
| 写 store | 密码成功即 `createBatchSigningTask` | 整批结束才写 |
| 矿工费 profile | `eligibility.signable[0]` | 取首条选中 |
| 切菜单 | `dismissBatchPopups` + `exitSelectMode` | 弹窗残留 |

**关键文件**：`useSigningBatchFlow.ts`、`SigningBatchFlowHost.vue`、`SigningBatchNetworkPickerMenu.vue`、`SigningBatchSignConfirmPopup.vue`、`SigningBatchProgressPopup.vue`、`batchSigning.constants.ts`。

### 7.3 多签

**MultiSignWaiting 成员态**

| presence | Tag | 设备标签 |
|----------|-----|----------|
| `joined` | 已加入 success | 有 |
| `pending` | 待加入 warning | **无** |
| `offline` | 无 | 无 |

**【必须】** 演示 `joinedCount = Math.max(1, required - 1)`。**【禁止】** `joinedCount = required`。

**多签邀请浮标**（`MultiSignInvitationFloatHost.vue`）

**【必须】** `EgAnchoredTooltip click-toggle="false"`；`onBadgeClick` 在 `button[data-eds-trigger-metrics]`；开前先 `closeAllAnchoredTooltips()`。Popup/批选/离页时 `dismissPanel()` 或 `v-if` 卸载（**禁止** `v-show` 留 teleport）。

### 7.4 付款钱包列

**【定稿】** 第 3 行（`rowIndex === 2`）长名 `Payroll Payout Wallet` 溢出演示；其余短名。`titleTextSlot`（`flex: 0 1 auto`）包 `EgListFieldOverflowText`，Tag 紧跟文字（**禁止** host-flex 占满把 Tag 推到列右）。

### 7.5 Shell Debug（DEV only）

**【必须】** 壳外工具与 `.app-preview` 业务 **互不影响**：调 Inspect / QA / Popover 只改 `src/dev/shell-debug/**`（及 `AppRoot.vue` 的 DEV 挂载）；**【禁止】** 为壳外改 `src/views/**`、`src/scenes/**`、业务布局/CSS。业务 **【禁止】** import `src/dev/**` 或为 Dev 加 `if (DEV)`。

**【允许】** 只改 `src/dev/shell-debug/**`；import 业务已 export API。**【禁止】** 业务 `import src/dev/**`；为 QA 改 `src/scenes/**`。

**Inspect 命名（点谁是谁）** — 真源 `src/dev/shell-debug/inspect/inspectNamingRules.ts`，五条规则按序判定：

1. 原子图形宿主（`.eds-icon` / `.eds-crypto` / `.eds-avatar`）→ 宿主组件
2. 节点自身 catalog `eds-*` 根类 **或** 节点就是**任意** DS 包组件 Vue DOM 根 → 该组件 + props
3. 排版文本叶子 → `Text`
4. DS 组件 CSS Module 的具名 Figma 区域（`Pagination`、`Apply_Item`）→ 区域名
5. 其余 → HTML 标签（`Div` / `Span` / `Td`…）

**【必须】** 一层一名：内层要不要组件名，只看 **它自己是什么** —— 自己是 DS 组件 → 组件名；自己只有 CSS Module 类 → HTML 标签。**「来自设计系统」= 节点自己就是 DS 组件**，不是「被 DS 组件渲染」（`_functionalGroup_` 由 `ToolBar.vue` 渲染，但它自己只是 `<div>` → `Div`）。

**点不到的父层走属性面板首行「祖先」**（`resolveInspectAncestorName.ts`）：`ToolBar.module.css` 的 `.root` / `.chrome` 无内边距、`width: 100%`，被 `.raw` 完全铺满，`header.eds-tool-bar` 没有可点像素 —— 祖先存在只是点不到，故单独成一行属性。**【必须】** 取最近**具名**层（纯标签层返回 `null`）；恒为**第一条** item；组件 props 与元素属性**两条路径**都加。**【禁止】** 祖先回流进 `primaryLabel` / `componentChain` / props。**【禁止】** 重提「外壳类借名」方案（白名单无法稳定界定，业务也会打 `eds-popup-inner-backdrop`）。

**【禁止】** 任何形式的祖先取名 —— `root.contains(element)`、沿 `parentElement` 回溯最近具名层取**名字**、按渲染归属（`__vnode.ctx`）继承。三者都会让父级与子级同名（如 ToolBar 根与内部 `_functional_`），属性与代码片段随之取到同一份参数。组件内部普通容器（`_stage_` / `_functional_` / `_itemTitle_` / `_cellContent_`）在 DS 里就是 `<div>` → 走规则 5。

**【必须】** R2 覆盖**任意** DS 包组件根（DS 105 个组件里 33 个未入 catalog，用 dev 的 `__file` 判定）。未入 catalog 只是「没有精选 props」，**【禁止】** 让其根退化成 HTML 标签 —— 否则 `Verify` / `CryptoAddress` / `MinerFee*Panel` 会显示成外层 `Popup`。

**【必须】** 同组件多 Figma 角色走 catalog `resolveDisplayName` hook（现有：`EgTooltip` 按 `panelKind` → `PopupBox` / `ContainerBox`，依据 tokens `effect/semantic.json` 的 `title`），**【禁止】** 在 resolver 写逐组件 `if` 或直读 `entry.displayName` 旁路。

**【禁止】** R4 区域层复制组件 props（props 只属规则 2 组件根）；**【禁止】** 为普通容器编造区域名或与 catalog 组件重名；**【禁止】** UI 层另起命名 fallback。

**【必须】** 改 Inspect 后跑 `node scripts/verify-shell-debug-inspect-naming.mjs`（12 项不变量，已接 `predev` / `prebuild`）。详案：`src/dev/shell-debug/AGENTS.md`。

### 7.6 DataList 批选模式 · 宽度与出入场动效【定稿】

**适用范围**：所有使用 `EgDataList select-mode` / Batch Bar 的页面，不限 Tasks。  
**DS 真源**：`../eds-desktop/packages/components/src/organisms/data-list/DataList.vue`、`DataList.module.css`、`useResponsiveColumns.ts`、`src/utils/overflowMeasureDuringLayout.ts`。  
**EDS 交接包**：`docs/eds-data-list-selection-layout-motion-handoff.md`。  
**业务边界**：这是 DS 布局能力；业务只控制 `selectMode`、选中数据和批处理流程，**禁止**在 `src/**` 重写列宽动画、覆盖 `EgDataList` 内部 DOM/CSS 或复制一套 DataList。

#### 7.6.1 目标与状态模型

批选切换不是「显示/隐藏 checkbox」这么简单，而是两套稳定布局之间的连续过渡：

| 端点 | 勾选列宽 | 数据列集合 | 数据列目标宽度 |
|------|----------|------------|----------------|
| 常态 `idle` | `0` | `idleSlotIndices`（常态可容纳列） | `idleColumnWidthsBySlot` |
| 批选 `select` | `40` | DOM 仍保留 `idleSlotIndices` | `selectModeColumnWidthsBySlot`；批选容纳不下的列目标宽度为 `0` |

**【必须】** 两端先独立算成稳定布局，再由原生 CSS 在两个目标值间过渡；不得在动画过程中反复运行响应式选列算法。  
**【必须】** 列宽总预算始终满足 `容器宽 - 勾选列占位 - DATA_LIST_FLEX_RESERVE_PX`；固定列、flex 列、尾列的既有分配规则保持不变。  
**【必须】** 入场与离场使用同一距离、同一时长、同一 easing；离场是入场的反向状态变更，不另造一套参数。

#### 7.6.2 实现方式

1. **常态列集常驻**
   - `visibleSlotIndices = idleSlotIndices`，常态可见的数据列在批选期间也保留在 DOM。
   - `selectModeSlotIndices` 只用于计算批选端点宽度，不用于动画中 `v-if` 卸载列。
   - 批选容纳不下的列在 `select` 端点取 `0px`；对应 header/cell 仍挂载，由 table `overflow: hidden` 裁切。

2. **勾选列常驻**
   - 勾选 `<col>`、表头选择单元格、每行选择单元格始终挂载。
   - 常态目标宽 `0px`，批选目标宽 `40px`；内容 opacity 为 `0 ↔ 1`、translateX 为 `-16px ↔ 0`。
   - 常态通过 `.rootSelectMode` 之外的 `pointer-events: none` 禁用交互；不得靠卸载解决点击穿透。

3. **一次响应式更新，浏览器负责逐帧**
   - Vue 只在 `selectMode` 改变时更新一组目标宽度。
   - `<col class="layoutColumn">` 使用 `width var(--motion-duration-shift) var(--motion-easing-standard)`。
   - checkbox 内容的 opacity / transform 使用相同时长和 easing。
   - Batch Bar 的 enter/leave 均使用同一 `--motion-recipe-float`；不得让 Bar 与表格使用互相冲突的时序。
   - **【禁止】** 用 `requestAnimationFrame` 每帧写 Vue `ref` / `computed` 宽度；这会使整表及业务 slot 每帧重新生成 VNode。

4. **选中态的逻辑值与退场画面分离**
   - `selectedList` 是业务逻辑真值；退出时立即清空并 emit `update:selected-list` / `selected-change`，外部流程时序不延后。
   - `selectionExitSnapshot` 只服务退场渲染：checkbox、表头三态、Batch Bar count 在 300ms 内维持退出前画面。
   - 动画结束后清 snapshot；重新进入、数据源变化、卸载时清 timer / snapshot，防止旧选择回流。

5. **动画期测量冻结**
   - 切换时设置 `data-eds-data-list-layout-animating`；`overflowMeasureDuringLayout.ts` 在动画期间冻结文本溢出测量。
   - CSS 时长 `--motion-duration-shift` 当前为 300ms；`SELECT_COLUMN_ANIM_MS` 必须与其相等，`DATA_LIST_LAYOUT_SETTLE_MS` 保持 320ms（动画时长 + 测量缓冲）。
   - 改动任一时长时，**必须**同步检查三处；禁止只改 CSS 或只改 timer。

6. **无障碍与降级**
   - `prefers-reduced-motion: reduce` 时 `<col>`、opacity、transform transition 全部关闭。
   - reduced motion 下不得保留 300ms 的逻辑阻塞；`selectAnimating` 应立即 settle。

#### 7.6.3 为什么必须常驻

退出批选时，常态通常会恢复一整列业务组件（每页多行 `EgCrypto`、Tooltip、地址/金额封装等）。若离场点击后才挂载：

1. `nextTick()` 会先同步创建整列 VNode / DOM，用户感知为「点了没反应」；
2. 若在第一个 rAF 才挂载，长帧会吞掉动画起步，下一帧直接跳到中间位置；
3. 若列显隐绑定 `selectProgress >= 1`，列集会在阈值帧突变，数学上的宽度连续也会被 DOM 挂载卡顿破坏；
4. 若动画结束后才挂载，主体动画看似正常，但结束后整表会再跳一次。

因此定稿为：**用少量常驻 DOM 换取切换路径零挂载；目标值一次更新，逐帧工作交给浏览器 CSS transition。**

#### 7.6.4 禁止回退清单

| 禁止方案 | 直接后果 |
|----------|----------|
| `selectColumnInDom` + `v-if` 在出入场挂卸勾选列 | 行数越多，首帧越卡 |
| 在批选停态卸载被挤掉的数据列，退出前 `await nextTick()` 挂回 | 点击后出现明显延迟 |
| `visibleSlotIndices` 跟随动画进度实时变化 | 阈值帧列集突变、掉帧 |
| rAF 每帧更新 `selectOffsetPx` 等响应式状态 | Vue 每帧重渲染整表和业务 slot |
| 入场 16px、离场 32px，或 enter/exit 不同 easing | 两方向视觉不对称 |
| 退出第一帧直接让渲染层读取已清空的 `selectedList` | checkbox、计数先闪成 0 再退场 |
| 普通关闭通过改 `:key` 强制 remount 整个 DataList | 动效消失、滚动/排序/测量状态丢失 |
| 业务 CSS 覆盖 `<col>`、header/cell 或另写 transition | DS 与业务时序分叉，无法复用 |
| 动画中持续触发 overflow Tooltip 测量 | 布局抖动与额外主线程开销 |

`TasksDataListPage.vue` 的 `dataListRemountKey` 只允许用于「提交成功后数据集整体换代且需隔离旧动画」的既有特殊链路，**禁止**扩展到 Batch Bar 关闭、批处理按钮 toggle、Escape 等普通退出路径。

#### 7.6.5 可复用契约

业务接入新批处理页面时只需：

```vue
<EgDataList
  v-model:select-mode="selectMode"
  :data-list="rows"
  @update:selected-list="selectedRows = $event"
/>
```

- 不需要也不允许获知 40px、16px、300ms、响应式列阈值或 `<col>` 结构。
- 多币种选择、批量审批、批量签名等入口最终都只切换同一个 `selectMode`。
- Batch Bar 关闭、工具栏按钮再次点击、Escape、切菜单退出必须汇聚到同一 `setSelectMode(false)` 链；不得各自实现视觉关闭。
- 快速反复 toggle 时，CSS 从当前计算宽度反向过渡，不重新挂载列；timer 必须先 clear 再重启。
- Resize / Skid / 客户端断点改变时重算两个端点；动画期间不切列集，settle 后再恢复 overflow 测量。

#### 7.6.6 验收与改动检查

改 `EgDataList` 批选或响应式列逻辑后，至少验证：

- [ ] 4173 dev HMR 下，工具栏「批处理」入场与 Batch Bar Close 离场方向相反、节奏一致；
- [ ] 离场点击后首帧立即响应，无 `nextTick` 挂载等待，动画结束后无二次列跳变；
- [ ] 20+ 重业务行、0 行、虚拟列表、loading/blank row 均无 colspan 错位；
- [ ] 快速连续进入/退出不会残留 0 宽可点击单元格、旧 snapshot 或 timer；
- [ ] 宽屏、临界列阈值、窗口 resize、Skid 开关下列宽总和与右缘稳定；
- [ ] checkbox、表头三态、Batch Bar count 在退场中保持原值，业务回调仍立即收到空选中集；
- [ ] `prefers-reduced-motion` 无 transition 且无逻辑延迟；
- [ ] Tooltip/overflow 在 settle 后重新测量，无动画中闪烁；
- [ ] `pnpm --filter @eds/desktop-components typecheck` 与消费 App `pnpm typecheck` 通过。

### 7.7 DataList 列宽与 min-width【定稿】

**适用范围**：所有 `EgDataList` / `EgDataListColumn` 列表（含 Tasks 各模块菜单、我发起的、批处理进度表等）。  
**DS 真源**：`../eds-desktop/packages/components/src/organisms/data-list/DataList.vue`、`DataList.module.css`、`useResponsiveColumns.ts`。  
**业务 min-width 绑定**：`tasksDataListPageData.ts`（`DATA_LIST_FIGMA_COLUMNS`、`*_DATA_LIST_COLUMN_MIN_WIDTH`、`resolveTasksDataListPreviewColumnMinWidth`、`syncTasksDataListColumnMinWidths`）→ `TasksDataListPage.vue` 的 `:min-width` / `:flex-grow`。

#### 7.7.1 min-width 语义（硬下限）

**【必须】** `EgDataListColumn` 的 `min-width` 决定该列**至少**能展示多少内容；布局算法与 `<col width>` **不得低于**此值。  
**【禁止】** 为 flex 列写 CSS `max-width`（含历史 `max-width: 0` 压列 hack）；数据列省略只靠 `overflow: hidden` + `text-overflow: ellipsis` + `<col width>`。  
**【禁止】** 在 `src/**` 用业务 CSS 覆盖 `<col>` / `th` / `td` 宽度或写 `max-width` 压列。

#### 7.7.2 flex 分配

| 列类型 | 行为 |
|--------|------|
| **flex 列**（无固定 `width`、且非「尾列 + flexGrow」） | 先取 `min-width`；容器有余量时在 min 之上**均分** extra |
| **固定尾列**（如 Action，或显式 `width`） | 不参与 flex 均分，保持 `width` / `min-width` |
| **Σ flex min > 可用预算** | 各列仍保持 min，**不压窄**；表格 `min-width = Σ列宽 + 勾选列`，`.tableContent` 横向滚动 |

可用预算：`DataList 容器宽 − 尾列固定宽 − DATA_LIST_FLEX_RESERVE_PX（80px）`。

#### 7.7.3 批选与 responsive 隐藏

- **批选**：勾选列独占 `40px`；**已可见数据列宽度与 idle 相同**（不因批选压缩低于 min）。容纳不下的列仍由 `display-order` / `getVisibleColumnSlotIndices` **隐藏**（`<col width>` → `0`），不是把可见列压到 min 以下。
- **窄屏 / Skid**：优先按 `display-order` 隐藏整列；**禁止**用 `max-width` 或低于 min 的 `<col width>` 硬挤可见列。

#### 7.7.4 业务接入

- **【必须】** 列 min-width 只通过 `EgDataListColumn :min-width`（及模块 helper）传入；Dev Inspect「DataList 适配」仅展示该 **prop 声明值**与是否 `（flex）`，**不展示**运行时 `<col>` 实测宽。
- **【必须】** 切模块菜单时调用 `syncTasksDataListColumnMinWidths`，保证 Shell Debug / Dev 适配参数与页面 `:min-width` 一致（见 `tasksDataListPageData.ts`）。
- **【禁止】** 只改 Dev 适配展示或 Inspect 读 DOM 宽度来代替 min-width 契约。

#### 7.7.5 验收

- [ ] Pin Inspect 某数据列：适配行仅为 `min-width: Npx` 或 `min-width: Npx（flex）`，无「实际 col」后缀。
- [ ] 容器变窄时可见列 `<col width>` ≥ 对应 `min-width`；必要时出现横向滚动，而非列宽低于 min。
- [ ] 容器变宽时 flex 列同步变宽（min 之上均分 extra）。
- [ ] 批选入场/离场：可见数据列宽不低于 min；仅勾选列 0↔40px 过渡。

---

## 8. 校验与 PR

**【必须】** `pnpm lint:ds-tokens`（脚本 `scripts/lint-ds-tokens.mjs`）。

**PR 核心项**

- [ ] 无 `@eds/website-*`、无 showcase CSS 复制
- [ ] 无 `Eg*` 根覆盖、无私造 `--*`
- [ ] 浮层用 `EgFlotation`；Popup 用 `usePopupShellLifecycle`
- [ ] 内联关闭只 `popupOpen=false`（§7.1）
- [ ] UI 文案走 `ui()`；字段 value 不 `ui()`（§5）
- [ ] 展示数字千分位（§6.2）
- [ ] Skid / `useScrollChromeScrim`（§6.3）
- [ ] 网络 Tag 链全名（§6.4）
- [ ] DataList 批选列常驻 + CSS `<col>` 过渡，无 rAF 响应式逐帧 / 普通退出 remount（§7.6）
- [ ] DataList 列宽不低于 `min-width`、无列 `max-width` 压窄、窄容器横滚（§7.7）
- [ ] dev HMR 已目测（§2.3）
- [ ] `listFieldCryptoSampleAddresses.ts` 业务扩展仍在
- [ ] 若本次含 DS 改动：远程 eds-desktop 已含对应提交；CI pin 与该 SHA 一致（§2.5）

---

## 9. 文件索引

| 主题 | 路径 |
|------|------|
| Agent 入口摘要 | `AGENTS.md` |
| EDS 完整约定 | `../eds-desktop/.cursor/rules/eds-project.mdc` |
| 顶底毛玻璃 DS 真源 | `../eds-desktop/.cursor/rules/eds-project.mdc` §13 |
| Popup lifecycle | `src/scenes/tasks/shared/usePopupShellLifecycle.ts` |
| Scroll scrim | `src/scenes/tasks/shared/useScrollChromeScrim.ts` |
| 千分位 | `src/utils/formatGroupedDisplay.ts` |
| i18n 运行时 | `src/i18n/translateUiText.ts` |
| 批处理 | `src/scenes/tasks/signing/batch/useSigningBatchFlow.ts` |
| DataList 批选宽度动效 | `../eds-desktop/packages/components/src/organisms/data-list/DataList.vue`、`DataList.module.css` |
| DataList 列 min-width / flex | §7.7；`DataList.vue`、`tasksDataListPageData.ts` |
| DataList 动效 EDS 交接包 | `docs/eds-data-list-selection-layout-motion-handoff.md` |
| list-field 校验 | `scripts/verify-list-field-crypto.mjs` |
| Shell Debug | `src/dev/shell-debug/AGENTS.md` |
| Pages CI pin | `.github/workflows/deploy-pages.yml`（`Checkout eds-desktop` 的 `ref`） |

---

## 10. 版本

| 字段 | 值 |
|------|-----|
| 文档版本 | 2.4 |
| 合并 `i18n.mdc` + `top&botton-mask.mdc` + 历史分散规则 → 单文件 | 2026-08-18 |
| 增补 §2.5 Git 推送 / Pages pin | 2026-08-19 |
| 增补 §2.5 4173/4174 双端口 parity（发包前一致） | 2026-08-21 |
| 增补文首 Agent 工作思路（先思考再动手） | 2026-08-21 |
| 增补 §7.6 DataList 批选宽度 / 出入场动效定稿 | 2026-09-01 |
| 增补 §7.7 DataList 列 min-width / flex 分配 | 2026-09-02 |
| 维护 | EDS Desktop 维护者 |

**本规范为业务项目强制约束。违反项须在 PR 修正或取得维护者书面例外。**
