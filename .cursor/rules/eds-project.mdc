---
description: 遵守 eds-desktop 项目约定 — token 语义映射、库分层、命名与滚动边界
globs: src/**/*
alwaysApply: true
---

# work-cregis-desktop — EDS 约定

本业务应用 **必须遵守** 同级目录设计系统仓库中的完整约定：

`/Users/DanBaby/Projects/eds-desktop/.cursor/rules/eds-project.mdc`

以及同目录下的专项规则（颜色、圆角、描边、Showcase 作用域等）。

## 对本项目的适用说明

1. **Desktop 层**：页面与业务 UI 一律使用 `@eds/desktop-tokens` / `@eds/desktop-components`；样式数值须走语义 token（`--spacing-*`、`--radius-*`、`--eds-body-*` 等），禁止因数值相同而误用 `--scale-*`。
2. **禁止硬编码**：无对应 token 时先停止并向维护者确认，不得擅自写裸 `px` / 颜色 / 字重。
3. **颜色**：仅通过 `var(--*)` 消费，遵守 Display P3 规则。
4. **嵌套滚动**：带内部滚动区的业务容器，默认按 eds-project §6（`overscroll-behavior: none` + wheel 拦截 + scrollTop 钳制）。
5. **浏览器预览框**：默认 `1280 × 800`，最小 `960 × 720`，集中定义于 `src/styles/global.css` 的 `--app-preview-*`；不属于 DS token，不得扩散到其他样式。

变更 eds-desktop 约定时，以 **eds-desktop 仓库源文件** 为准。
